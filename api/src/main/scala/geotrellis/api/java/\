package geotrellis.api.java

import java.{lang => jl}
import java.util.{Comparator, Iterator => JIterator, List => JList, Map => JMap}

import scala.collection.JavaConverters._
import scala.reflect.ClassTag

import geotrellis.raster._
import geotrellis.raster.io.geotiff._

import geotrellis.vector._
import geotrellis.proj4._

abstract class JavaSourceTile[K, V <: Tile] extends JavaSourceTileLike[K, V]

trait JavaSourceTileLike[K, V <: Tile] extends Serializable
