import trellis.dsl.MapAlgebra._
import trellis.process._
import trellis.operation.int._

val s = Server("test")

val Add5Operation = operation { r:IntRasterOperation => AddConstant(r, 5) }
val O = Add5Operation(LoadFile("/var/trellis/data/foo.arg32"))

val result = O.run(s) 
